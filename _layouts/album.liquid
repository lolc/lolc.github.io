---
layout: default
---

<div class="post">

  <header class="post-header">
    <h1 class="post-title">photo albums</h1>
    <p class="post-description">{{ page.description }}</p>
  </header>

  <article>
    {{ content }}
    <ul>
      {% comment %}
      Get all "photo_set" pages and display a list with links to them.
      {% endcomment %}
      {% assign photo_pages = site.pages | where: "layout", "photo_set" %}
      {% assign photo_pages2 = photo_pages | sort:"title" %}
      {% for photo_page in photo_pages2 reversed %}
      <li>
	<a href="{{ photo_page.url | prepend: site.baseurl }}">{{ photo_page.titlelong }}</a>
      </li>
      {% endfor %}
    </ul>
    
  </article>

</div>
